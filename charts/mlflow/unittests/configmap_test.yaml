suite: test configmap

templates:
  - configmap.yaml

release:
  name: mlflow
  namespace: mlflow

chart:
  version: 1.0.0
  appVersion: 1.0.0

tests:
  - it: should have postgres environments when postgres is enabled
    set:
      backendStore.postgres.enabled: true
      backendStore.postgres.host: postgres-service
      backendStore.postgres.port: 5432
      backendStore.postgres.database: postgres
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: data.PGHOST
          value: postgres-service
      - equal:
          path: data.PGPORT
          value: "5432"
      - equal:
          path: data.PGDATABASE
          value: postgres

  - it: should have postgres environments and migration configmaps when postgres and migration are enabled
    set:
      backendStore.postgres.enabled: true
      backendStore.postgres.host: postgres-service
      backendStore.postgres.port: 5432
      backendStore.postgres.database: postgres
      backendStore.databaseMigration: true
    asserts:
      - hasDocuments:
          count: 2

  - it: should have postgres environments and database connection checker configmaps when postgres and database connection check are enabled
    set:
      backendStore.postgres.enabled: true
      backendStore.postgres.host: postgres-service
      backendStore.postgres.port: 5432
      backendStore.postgres.database: postgres
      backendStore.databaseConnectionCheck: true
    asserts:
      - hasDocuments:
          count: 2

  - it: should have mysql environments when mysql is enabled
    set:
      backendStore.mysql.enabled: true
      backendStore.mysql.host: mysql-service
      backendStore.mysql.port: 3306
      backendStore.mysql.database: mysql
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: data.MYSQL_HOST
          value: mysql-service
      - equal:
          path: data.MYSQL_TCP_PORT
          value: "3306"
      - equal:
          path: data.MYSQL_DATABASE
          value: mysql

  - it: should have mysql environments when bitnami mysql is enabled
    set:
      mysql:
        enabled: true
        auth:
          database: custom-db
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: data.MYSQL_HOST
          value: mlflow-mysql
      - equal:
          path: data.MYSQL_TCP_PORT
          value: "3306"
      - equal:
          path: data.MYSQL_DATABASE
          value: custom-db

  - it: should have mysql environments and migration configmaps when mysql and migration are enabled
    set:
      backendStore.mysql.enabled: true
      backendStore.mysql.host: mysql-service
      backendStore.mysql.port: 3306
      backendStore.mysql.database: mysql
      backendStore.databaseMigration: true
    asserts:
      - hasDocuments:
          count: 2

  - it: should have mysql environments and migration configmaps when bitnamimysql and migration are enabled
    set:
      mysql:
        enabled: true
        auth:
          database: custom-db
      backendStore.databaseMigration: true
    asserts:
      - hasDocuments:
          count: 2

  - it: should have mssql environments when mssql is enabled
    set:
      backendStore.mssql.enabled: true
      backendStore.mssql.host: mssql-service
      backendStore.mssql.port: 5555
      backendStore.mssql.database: mssql
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: data.MSSQL_HOST
          value: mssql-service
      - equal:
          path: data.MSSQL_TCP_PORT
          value: "5555"
      - equal:
          path: data.MSSQL_DATABASE
          value: mssql

  - it: should have mssql environments and migration configmaps when mssql and migration are enabled
    set:
      backendStore.mssql.enabled: true
      backendStore.mssql.host: mssql-service
      backendStore.mssql.port: 5555
      backendStore.mssql.database: mssql
      backendStore.databaseMigration: true
    asserts:
      - hasDocuments:
          count: 2

  - it: should skip mssql host port database env vars when connectionUrl is set
    set:
      backendStore.mssql.enabled: true
      backendStore.mssql.connectionUrl: "mssql+pyodbc://?odbc_connect=DRIVER%3DODBC+Driver+17%3BAuthentication%3DActiveDirectoryMsi%3B"
    asserts:
      - notExists:
          path: data.MSSQL_HOST
        documentSelector:
          path: metadata.name
          value: mlflow-env-configmap
      - notExists:
          path: data.MSSQL_TCP_PORT
        documentSelector:
          path: metadata.name
          value: mlflow-env-configmap
      - notExists:
          path: data.MSSQL_DATABASE
        documentSelector:
          path: metadata.name
          value: mlflow-env-configmap

  - it: should use connectionUrl in migration script when mssql connectionUrl is set
    set:
      backendStore.mssql.enabled: true
      backendStore.mssql.connectionUrl: "mssql+pyodbc://?odbc_connect=DRIVER%3DODBC+Driver+17%3BAuthentication%3DActiveDirectoryMsi%3B"
      backendStore.databaseMigration: true
    asserts:
      - matchSnapshot: {}
        documentSelector:
          path: metadata.name
          value: mlflow-migrations

  - it: should skip mssql host port database env vars when existingConnectionUrlSecret is set
    set:
      backendStore.mssql.enabled: true
      backendStore.mssql.existingConnectionUrlSecret.name: mssql-url-secret
      backendStore.mssql.existingConnectionUrlSecret.key: connectionUrl
    asserts:
      - notExists:
          path: data.MSSQL_HOST
        documentSelector:
          path: metadata.name
          value: mlflow-env-configmap
      - notExists:
          path: data.MSSQL_TCP_PORT
        documentSelector:
          path: metadata.name
          value: mlflow-env-configmap
      - notExists:
          path: data.MSSQL_DATABASE
        documentSelector:
          path: metadata.name
          value: mlflow-env-configmap

  - it: should use existingConnectionUrlSecret in migration script when set
    set:
      backendStore.mssql.enabled: true
      backendStore.mssql.existingConnectionUrlSecret.name: mssql-url-secret
      backendStore.mssql.existingConnectionUrlSecret.key: connectionUrl
      backendStore.databaseMigration: true
    asserts:
      - matchSnapshot: {}
        documentSelector:
          path: metadata.name
          value: mlflow-migrations

  - it: should have postgresql environments when postgresql is enabled
    set:
      postgresql:
        enabled: true
        auth:
          database: custom-db
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.PGHOST
          value: mlflow-postgresql
      - equal:
          path: data.PGPORT
          value: "5432"
      - equal:
          path: data.PGDATABASE
          value: custom-db

  - it: should use different postgresql port when postgresql is enabled and postgresql.primary.service.ports.postgresql is set
    set:
      postgresql:
        enabled: true
        primary:
          service:
            ports:
              postgresql: 5433
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.PGPORT
          value: "5433"

  - it: should have postgresql environments and migration configmaps when postgres and migration are enabled
    set:
      postgresql:
        enabled: true
      backendStore:
        databaseMigration: true
    asserts:
      - hasDocuments:
          count: 2

  - it: should have postgresql environments and database connection checker configmaps when postgres and database connection check are enabled
    set:
      postgresql:
        enabled: true
      backendStore:
        databaseConnectionCheck: true
    asserts:
      - hasDocuments:
          count: 2

  - it: should have log level configuration when log level is set
    set:
      log:
        enabled: true
        level: debug
    asserts:
      - equal:
          path: data.MLFLOW_CONFIGURE_LOGGING
          value: "true"
      - equal:
          path: data.MLFLOW_LOGGING_LEVEL
          value: DEBUG

  - it: should disable telemetry on default
    asserts:
      - equal:
          path: data.MLFLOW_DISABLE_TELEMETRY
          value: "true"
      - equal:
          path: data.DO_NOT_TRACK
          value: "true"

  - it: should inject MLFLOW_SERVER_ALLOWED_HOSTS when ingress is enabled with a host
    set:
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_ALLOWED_HOSTS
          value: "mlflow.example.com"

  - it: should join multiple ingress hosts with comma
    set:
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
          - host: mlflow2.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_ALLOWED_HOSTS
          value: "mlflow.example.com,mlflow2.example.com"

  - it: should still inject MLFLOW_SERVER_ALLOWED_HOSTS into configmap even when user sets it in extraEnvVars
    set:
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
      extraEnvVars:
        MLFLOW_SERVER_ALLOWED_HOSTS: "*"
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_ALLOWED_HOSTS
          value: "mlflow.example.com"

  - it: should not inject MLFLOW_SERVER_ALLOWED_HOSTS when ingress is disabled
    set:
      ingress.enabled: false
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - notExists:
          path: data.MLFLOW_SERVER_ALLOWED_HOSTS

  - it: should append serverAllowedHosts to ingress-derived hosts and deduplicate
    set:
      serverAllowedHosts:
        - mlflow.example.com
        - dashboard.example.com
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_ALLOWED_HOSTS
          value: "mlflow.example.com,dashboard.example.com"

  - it: should append extra serverAllowedHosts not present in ingress
    set:
      serverAllowedHosts:
        - dashboard.example.com
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_ALLOWED_HOSTS
          value: "mlflow.example.com,dashboard.example.com"

  - it: should output only wildcard when serverAllowedHosts contains asterisk
    set:
      serverAllowedHosts:
        - "*"
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_ALLOWED_HOSTS
          value: "*"

  - it: should inject serverAllowedHosts even when ingress is disabled
    set:
      serverAllowedHosts:
        - mlflow.internal
        - 10.0.0.5
      ingress.enabled: false
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_ALLOWED_HOSTS
          value: "mlflow.internal,10.0.0.5"

  - it: should auto-inject http CORS origin from ingress host when no TLS
    set:
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_CORS_ALLOWED_ORIGINS
          value: "http://mlflow.example.com"

  - it: should auto-inject https CORS origin from ingress host when TLS configured
    set:
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
        tls:
          - secretName: mlflow-tls
            hosts:
              - mlflow.example.com
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_CORS_ALLOWED_ORIGINS
          value: "https://mlflow.example.com"

  - it: should join multiple ingress hosts for CORS origins with comma
    set:
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
          - host: mlflow2.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_CORS_ALLOWED_ORIGINS
          value: "http://mlflow.example.com,http://mlflow2.example.com"

  - it: should append extra corsAllowedOrigins not present in ingress
    set:
      corsAllowedOrigins:
        - https://dashboard.example.com
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_CORS_ALLOWED_ORIGINS
          value: "http://mlflow.example.com,https://dashboard.example.com"

  - it: should deduplicate when corsAllowedOrigins repeats an ingress-derived origin
    set:
      corsAllowedOrigins:
        - http://mlflow.example.com
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_CORS_ALLOWED_ORIGINS
          value: "http://mlflow.example.com"

  - it: should output only wildcard when corsAllowedOrigins contains asterisk
    set:
      corsAllowedOrigins:
        - "*"
      ingress:
        enabled: true
        hosts:
          - host: mlflow.example.com
            paths:
              - path: /
                pathType: ImplementationSpecific
    documentSelector:
      path: metadata.name
      value: mlflow-env-configmap
    asserts:
      - equal:
          path: data.MLFLOW_SERVER_CORS_ALLOWED_ORIGINS
          value: "*"

  - it: should match snapshot of default values
    release:
      name: mlflow
      namespace: mlflow
    chart:
      version: 1.0.0
      appVersion: 1.0.0
    asserts:
      - matchSnapshot: { }

  - it: should match snapshot of default values with postgres
    set:
      backendStore:
        postgres:
          enabled: true
          host: postgres-custom-service
          port: 5432
          database: mlflow
          user: mlflowuser
          password: mlflowpassword
    release:
      name: mlflow
      namespace: mlflow
    chart:
      version: 1.0.0
      appVersion: 1.0.0
    asserts:
      - matchSnapshot: { }

  - it: should match snapshot of default values with mysql
    set:
      backendStore:
        mysql:
          enabled: true
          host: mysql-custom-service
          port: 3306
          database: mlflow
          user: mlflowuser
          password: mlflowpassword
    release:
      name: mlflow
      namespace: mlflow
    chart:
      version: 1.0.0
      appVersion: 1.0.0
    asserts:
      - matchSnapshot: { }

  - it: should match snapshot of default values with bitnami postgresql
    set:
      postgresql:
        enabled: true
    release:
      name: mlflow
      namespace: mlflow
    chart:
      version: 1.0.0
      appVersion: 1.0.0
    asserts:
      - matchSnapshot: { }

  - it: should match snapshot of default values with bitnami mysql
    set:
      mysql:
        enabled: true
    release:
      name: mlflow
      namespace: mlflow
    chart:
      version: 1.0.0
      appVersion: 1.0.0
    asserts:
      - matchSnapshot: { }

  - it: should match snapshot of default values with mssql
    set:
      backendStore:
        mssql:
          enabled: true
          host: mssql-custom-service
          port: 5555
          database: mlflow
          user: mlflowuser
          password: mlflowpassword
    release:
      name: mlflow
      namespace: mlflow
    chart:
      version: 1.0.0
      appVersion: 1.0.0
    asserts:
      - matchSnapshot: { }
